<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:padding="24dp"
    android:background="@drawable/bg_bottom_sheet">

    <!-- Drag Handle -->
    <View
        android:layout_width="40dp"
        android:layout_height="4dp"
        android:layout_gravity="center_horizontal"
        android:layout_marginBottom="20dp"
        android:background="@color/border_gray" />

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Rekam Suara"
        android:textColor="@color/text_main"
        android:textSize="18sp"
        android:textStyle="bold"
        android:layout_gravity="center_horizontal"
        android:layout_marginBottom="8dp"/>

    <TextView
        android:id="@+id/tvVoiceStatus"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Mendengarkan..."
        android:textColor="@color/text_muted"
        android:textSize="14sp"
        android:layout_gravity="center_horizontal"
        android:layout_marginBottom="24dp"/>

    <!-- Mic Icon with Pulsing Circle Background -->
    <FrameLayout
        android:layout_width="120dp"
        android:layout_height="120dp"
        android:layout_gravity="center_horizontal"
        android:layout_marginBottom="16dp">

        <!-- Outer pulse ring (animated programmatically) -->
        <View
            android:id="@+id/viewPulseOuter"
            android:layout_width="120dp"
            android:layout_height="120dp"
            android:layout_gravity="center"
            android:background="@drawable/bg_circle_icon"
            android:backgroundTint="@color/primary_light"
            android:alpha="0.3" />

        <!-- Inner pulse ring -->
        <View
            android:id="@+id/viewPulseInner"
            android:layout_width="90dp"
            android:layout_height="90dp"
            android:layout_gravity="center"
            android:background="@drawable/bg_circle_icon"
            android:backgroundTint="@color/primary_light"
            android:alpha="0.6" />

        <!-- Mic Icon Circle -->
        <ImageView
            android:id="@+id/ivRecordingMic"
            android:layout_width="64dp"
            android:layout_height="64dp"
            android:layout_gravity="center"
            android:background="@drawable/bg_circle_icon"
            android:backgroundTint="@color/primary"
            android:padding="16dp"
            android:src="@drawable/ic_mic"
            android:importantForAccessibility="no"
            app:tint="@color/white" />
    </FrameLayout>

    <!-- Partial Result Text -->
    <TextView
        android:id="@+id/tvPartialResult"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Silakan ucapkan transaksi Anda..."
        android:textColor="@color/text_main"
        android:textSize="15sp"
        android:textStyle="italic"
        android:gravity="center"
        android:minHeight="48dp"
        android:layout_marginBottom="24dp"/>

    <!-- Cancel Button -->
    <Button
        android:id="@+id/btnCancelRecording"
        android:layout_width="match_parent"
        android:layout_height="48dp"
        android:text="Batal"
        android:textColor="@color/text_main"
        android:textStyle="bold"
        android:background="@drawable/bg_brutal_button_secondary"
        app:backgroundTint="@null" />

</LinearLayout>
