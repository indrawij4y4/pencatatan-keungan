<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:paddingVertical="10dp"
    android:paddingHorizontal="16dp"
    android:background="@android:color/transparent">

    <!-- Category Icon Container -->
    <FrameLayout
        android:id="@+id/catIconContainer"
        android:layout_width="36dp"
        android:layout_height="36dp"
        android:background="@drawable/bg_circle_icon"
        android:backgroundTint="@color/primary_light"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent">

        <ImageView
            android:id="@+id/ivCatIcon"
            android:layout_width="18dp"
            android:layout_height="18dp"
            android:layout_gravity="center"
            tools:src="@drawable/ic_trending_down"
            app:tint="@color/primary" />
    </FrameLayout>

    <!-- Category Name -->
    <TextView
        android:id="@+id/tvCatName"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginStart="12dp"
        android:textColor="@color/text_main"
        android:textSize="14sp"
        android:textStyle="bold"
        app:layout_constraintEnd_toStartOf="@+id/tvCatPercentage"
        app:layout_constraintStart_toEndOf="@+id/catIconContainer"
        app:layout_constraintTop_toTopOf="@+id/catIconContainer"
        tools:text="Bahan Baku" />

    <!-- Percentage -->
    <TextView
        android:id="@+id/tvCatPercentage"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginEnd="8dp"
        android:textColor="@color/text_muted"
        android:textSize="13sp"
        app:layout_constraintBottom_toBottomOf="@+id/tvCatName"
        app:layout_constraintEnd_toStartOf="@+id/tvCatAmount"
        app:layout_constraintTop_toTopOf="@+id/tvCatName"
        tools:text="45%" />

    <!-- Amount -->
    <TextView
        android:id="@+id/tvCatAmount"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textColor="@color/text_main"
        android:textSize="14sp"
        android:textStyle="bold"
        app:layout_constraintBottom_toBottomOf="@+id/tvCatName"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="@+id/tvCatName"
        tools:text="Rp 2.500.000" />

    <!-- Progress Bar (Thin line below) -->
    <ProgressBar
        android:id="@+id/pbExpenseProgress"
        style="@style/Widget.AppCompat.ProgressBar.Horizontal"
        android:layout_width="0dp"
        android:layout_height="4dp"
        android:layout_marginTop="8dp"
        android:progressDrawable="@drawable/bg_progress_teal"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="@+id/tvCatName"
        app:layout_constraintTop_toBottomOf="@+id/tvCatName"
        tools:progress="45" />

</androidx.constraintlayout.widget.ConstraintLayout>
